#include<bits/stdc++.h>
#include<stdio.h>
#include<string.h>
using namespace std;
#define		pb              push_back
#define		PB              pop_back
#define		nn              "\n"
#define		all(p)          p.begin(),p.end()
#define		ss              " "
#define		gcd(a,b)        __gcd(a,b)
#define		lcm(a,b)        (a*b)/gcd(a,b)
#define		pi              acos(-1.0)
typedef		long long       ll;
typedef		vector<ll>      vll;
struct student
{
    char name[30];
    int phone,birth_month=2;
};
int main()
{
    student s, student[50];
    gets(s.name);
    cin>>(student[10].phone);
    cout<<(student[5].birth_month);
    return 0;
}

