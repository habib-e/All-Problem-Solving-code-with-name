#include<bits/stdc++.h>
#include<string.h>
using namespace std;
int main()
{
    char str[101];
    gets(str);
    int i,j;
    int len=strlen(str);
    for(i=0;i<str.size();i++)
    {
        if(str[i]=='A' || str[i]=='E' || str[i]=='I' || str[i]=='O' || str[i]=='Y' || str[i]=='U' ||str[i]=='a' ||
           str[i]=='e' || str[i]=='i' || str[i]=='o' ||str[i]=='u' || str[i]=='y')
    }


    return 0;
}
